{
  "name": "{;slug;}",
  "version": "0.1.0",
  "private": true,
  "!presets": ["ts", "babel", "react", "web", "graphql"],
  "scripts": {
    "start": "spin start",
    "build": "spin build",
    "test": "spin test"
  },
  "dependencies": {
    "apollo-boost": "^0.1.23"
  }
}
